# Proguard rules specific to the FFmpeg extension.

# This prevents the names of native methods from being obfuscated.
-keepclasseswithmembernames class * {
    native <methods>;
}

# This method is called from native code
-keep, includedescriptorclasses class com.google.android.exoplayer2.ext.ffmpeg.FfmpegAudioDecoder {
  private java.nio.ByteBuffer growOutputBuffer(com.google.android.exoplayer2.decoder.SimpleDecoderOutputBuffer, int);
}
